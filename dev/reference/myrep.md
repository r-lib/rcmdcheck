# Recycle a vector or a data frame (rows)

Recycle a vector or a data frame (rows)

## Usage

``` r
myrep(x, len)
```

## Arguments

- x:

  Vector or data frame to replicate. Must be length 0, 1, or len.

- len:

  Expected length.
